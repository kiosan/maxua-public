<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <title>Weekly Digest - Max Ischenko</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #222;
      margin: 0;
      padding: 0;
      -webkit-text-size-adjust: 100%;
    }
    
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 0;
    }
    
    .post-card {
      border: 1px solid #eee;
      padding: 1.25rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
      background-color: #f9f9f9;
    }
    
    .post-content {
      margin-bottom: 1rem;
      font-size: 1rem;
      white-space: pre-wrap;
      word-wrap: break-word;
    }
    
    .post-meta {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      font-size: 0.85rem;
      color: #555;
    }
    
    .post-date {
      color: #555;
    }
    
    .topic-badge {
      font-size: 0.9rem;
      padding: 0.25rem 0.6rem;
      background-color: #f0f4f8;
      color: #1a73e8;
      border-radius: 12px;
      text-decoration: none;
      display: inline-block;
      margin-left: 8px;
      font-weight: 500;
    }
    
    .post-link {
      text-decoration: none;
      color: inherit;
      display: block;
    }
    
    .post-image-container {
      margin: 1rem 0;
      max-width: 100%;
      border-radius: 8px;
      overflow: hidden;
    }
    
    .post-image {
      width: 100%;
      max-height: 300px;
      object-fit: contain;
      background-color: #f8f8f8;
      display: block;
    }
    
    .footer {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 1px solid #eee;
      font-size: 0.85rem;
      color: #666;
      text-align: center;
    }
    
    .footer a {
      color: #1a73e8;
      text-decoration: none;
    }
    
    .comments-badge {
      display: inline-flex;
      align-items: center;
      font-size: 0.85rem;
      color: #555;
      text-decoration: none;
      margin-left: auto;
    }
  </style>
</head>
<body>
  <div class="container">
    {{#if posts.length}}
      {{#each posts}}
      <div class="post-card">
        <a href="https://maxua.com/p/{{this.id}}" class="post-link">
          <div class="post-content">{{{this.content_html}}}</div>
          {{#if this.image_url}}
          <div class="post-image-container">
            <img src="{{this.image_url}}" alt="Post image" class="post-image">
          </div>
          {{/if}}
        </a>
        
        <div class="post-meta">
          <div class="post-date">
            {{this.formatted_date}}
            {{#if this.topic_name}}
            <a href="https://maxua.com/t/{{this.topic_slug}}" class="topic-badge">#{{this.topic_name}}</a>
            {{/if}}
            
            {{#if this.comment_count}}
            {{#if (neq this.comment_count 0)}}
            <a href="https://maxua.com/p/{{this.id}}#comments" class="comments-badge">
              💬 {{this.comment_count}}
            </a>
            {{/if}}
            {{/if}}
          </div>
        </div>
      </div>
      {{/each}}
    {{else}}
      <p style="text-align: center; color: #555; padding: 20px;">No new posts this week.</p>
    {{/if}}
    
    <div class="footer">
      <p>You're receiving this email because you subscribed to weekly updates from <a href="https://maxua.com">maxua.com</a>.</p>
      <p>
        <a href="https://maxua.com/api/unsubscribe?token=%%unsubscribeUrl%%">Unsubscribe</a> from these emails.
      </p>
    </div>
  </div>
</body>
</html>
